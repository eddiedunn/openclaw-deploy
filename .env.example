# =============================================================================
# OpenClaw Deployment Configuration
# =============================================================================
# Copy this file to .env and customize for your environment:
#   cp .env.example .env
#
# All scripts source this file automatically.
# =============================================================================

# --- Host ---
# Hostname of the server where OpenClaw is deployed
OPENCLAW_HOST=your-server

# --- Network ---
# LAN IP address of the host
OPENCLAW_LAN_IP=192.168.1.100
# Mesh/VPN IP address of the host (e.g., Tailscale/Headscale)
OPENCLAW_MESH_IP=100.64.0.x

# --- DNS ---
# Primary DNS server (e.g., MagicDNS for mesh networks)
OPENCLAW_DNS_PRIMARY=100.100.100.100
# Fallback DNS server
OPENCLAW_DNS_FALLBACK=1.1.1.1

# --- Service User ---
# System username for running OpenClaw
OPENCLAW_USER=openclaw
# UID for the service user
OPENCLAW_UID=993
# GID for the service user
OPENCLAW_GID=990
# Home directory for the service user
OPENCLAW_HOME=/data/openclaw

# --- Subuid/Subgid ---
# Starting subordinate UID for rootless Podman (must not overlap with other users)
OPENCLAW_SUBUID_START=231072
# Number of subordinate UIDs to allocate
OPENCLAW_SUBUID_COUNT=65536

# --- Ports ---
# Gateway port (control UI + WebSocket)
OPENCLAW_GATEWAY_PORT=18789
# Bridge port
OPENCLAW_BRIDGE_PORT=18790

# --- Container ---
# Container image name
OPENCLAW_IMAGE=openclaw:local

# --- Resource Limits (default/primary instance) ---
# Memory limit for the default instance
OPENCLAW_MEMORY_DEFAULT=8g
# CPU limit for the default instance
OPENCLAW_CPUS_DEFAULT=4

# --- Resource Limits (additional instances) ---
# Memory limit for additional instances
OPENCLAW_MEMORY_INSTANCE=4g
# CPU limit for additional instances
OPENCLAW_CPUS_INSTANCE=2
